/* eslint-disable prettier/prettier */
import { Controller } from '@nestjs/common';

@Controller('task')
export class TaskController {
    // Add task
    // delete task
    // Update task
    // mark as Complete
    // list all task per user
}
